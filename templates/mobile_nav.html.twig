<div class="menu mr-1 lg:hidden" data-controller="sidebar">
    <div class="pl-5 pr-2  md:p-0 md:pl-0 md:pr-0">
        <button class="focus:outline-none" aria-label="Open Menu" data-action="click->sidebar#openSidebar">
            <i id="hambuger-icon" class="transition-all duration-500 ease-in-out">
                <svg fill="currentColor" viewBox="0 0 20 20" class="w-8 h-8" style="transform: rotateY(180deg);">
                    <path x-show="!open" fill-rule="evenodd" d="M3 5a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 10a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM9 15a1 1 0 011-1h6a1 1 0 110 2h-6a1 1 0 01-1-1z" clip-rule="evenodd"></path>
                </svg>
            </i>
        </button>
    </div>
    <nav class="flex fixed w-full items-center justify-between px-1 md:px-14 bg-white dark:bg-gray-700 text-gray-700 z-10">
        <transition enter-class="opacity-0" enter-active-class="ease-out transition-medium" enter-to-class="opacity-100" leave-class="opacity-100" leave-active-class="ease-out transition-medium" leave-to-class="opacity-0">
            <div data-sidebar-target="drawertransition" class="hidden z-10 fixed inset-0 transition-opacity">
                <div data-action="click->sidebar#resetOpen" class="absolute inset-0 bg-black opacity-50" tabindex="0"></div>
            </div>
        </transition>
        <aside class="transform top-0 left-0 w-72 dark:bg-gray-800 bg-white fixed h-full overflow-auto ease-in-out transition-all duration-300 z-50 -translate-x-full" data-sidebar-target="draweraside">
            <span data-action="click->sidebar#resetOpen" class="flex w-full items-center p-3">
                <a href="{{ path("home")}}" class="w-full lg:w-auto flex title-font font-medium items-center dark:text-white text-gray-900 mb-4 md:mb-0">
                    <span class="text-deep-orange-500 font-black text-2xl">Buyers</span>
                   <span class="font-black mt-2 text-lg sm:text-2xl">and</span>
                   <span class="text-deep-orange-500 font-black text-2xl">Sellers</span> 
                   <span class="font-black text-2xl">.ng</span>
                </a>
            </span>
            
            {% include 'nav_links.html.twig' %}
        </aside>
    </nav>
</div>